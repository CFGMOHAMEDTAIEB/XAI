[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "xai-compress"
version = "0.2.0"
description = "Hybrid neural/classical lossless compression with arithmetic and rANS coding"
requires-python = ">=3.10"
dependencies = ["torch>=2.2", "numpy>=1.26"]

[project.scripts]
xai-compress = "xai_compress.cli:main"
xcompress = "xai_compress.cli:main"

[tool.setuptools.packages.find]
include = ["xai_compress*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
