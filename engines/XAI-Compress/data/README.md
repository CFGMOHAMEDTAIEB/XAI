# Local training data
Do not put training corpora in Git or in the delivery ZIP.
Place your files under `data/train/` and held-out files under `data/test/`.
Archives are excluded by default. Use `--include-archives` only deliberately.
