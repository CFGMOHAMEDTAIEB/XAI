events {}
http {
  limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
  server { listen 80; client_max_body_size 256m;
    location /api/ { limit_req zone=api burst=20; proxy_pass http://api:8000/; proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for; }
  }
}
