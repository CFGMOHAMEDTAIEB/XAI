# Angular Web Portal Skeleton

Create a full Angular workspace with `ng new`, then copy `src/app/app.component.ts` and merge the package dependencies. Planned modules: OIDC login, cloud jobs, sharing, inbox, history, devices, security, quotas, and admin views.
