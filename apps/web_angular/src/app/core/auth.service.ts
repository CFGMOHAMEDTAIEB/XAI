import {Injectable,computed,signal} from '@angular/core';import {HttpClient} from '@angular/common/http';import {Router} from '@angular/router';import {firstValueFrom} from 'rxjs';import {environment} from '../../environments/environment';import {AuthToken} from '../models/models';
@Injectable({providedIn:'root'})export class AuthService{private readonly key='xai_access_token';private readonly refreshKey='xai_refresh_token';private readonly value=signal<string|null>(localStorage.getItem(this.key));readonly token=this.value.asReadonly();readonly authenticated=computed(()=>this.value()!==null);constructor(private http:HttpClient,private router:Router){}async register(email:string,password:string){const r=await firstValueFrom(this.http.post<AuthToken>(`${environment.apiUrl}/auth/register`,{email,password}));this.save(r.access_token,r.refresh_token)}async login(email:string,password:string,totp?:string){if(environment.demoMode&&email==='demo@xai.local'){this.save('demo-token');return;}const response=await firstValueFrom(this.http.post<AuthToken>(`${environment.apiUrl}/auth/login`,{email,password,totp_code:totp||null}));this.save(response.access_token,response.refresh_token)}async refresh(){const refresh_token=localStorage.getItem(this.refreshKey);if(!refresh_token)throw new Error('No refresh token');const response=await firstValueFrom(this.http.post<AuthToken>(`${environment.apiUrl}/auth/refresh`,{refresh_token}));this.save(response.access_token,response.refresh_token);return response.access_token}logout(){const refresh_token=localStorage.getItem(this.refreshKey);if(refresh_token)this.http.post(`${environment.apiUrl}/auth/logout`,{refresh_token}).subscribe({error:()=>{}});localStorage.removeItem(this.key);localStorage.removeItem(this.refreshKey);this.value.set(null);void this.router.navigateByUrl('/login')}private save(token:string,refresh?:string){localStorage.setItem(this.key,token);if(refresh)localStorage.setItem(this.refreshKey,refresh);this.value.set(token)}}
