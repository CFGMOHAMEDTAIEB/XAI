import {environment} from '../../environments/environment';import {ApiService} from '../core/api.service';import {Component,signal} from '@angular/core';import {RouterLink,RouterLinkActive,RouterOutlet} from '@angular/router';import {AuthService} from '../core/auth.service';import {IconComponent} from '../ui/icon.component';
@Component({standalone:true,imports:[RouterOutlet,RouterLink,RouterLinkActive,IconComponent],template:`
<div class="shell">@if(drawer()){<button class="drawer-backdrop" aria-label="Close navigation" (click)="drawer.set(false)"></button>}<aside class="sidebar" [class.open]="drawer()"><a class="brand" routerLink="/dashboard" (click)="close()"><span>XC</span><b>XAI-Compress<small>Personal workspace</small></b></a><nav aria-label="Portal navigation">@for(item of nav;track item.path){<a [routerLink]="item.path" routerLinkActive="active" (click)="close()"><x-icon [name]="item.icon"/>{{item.label}}</a>}<a class="external" [href]="publicSiteUrl"><x-icon name="share"/>Public website</a>@if(isAdmin&&adminUrl){<a [href]="adminUrl"><x-icon name="security"/>Admin console</a>}</nav><button class="logout" (click)="auth.logout()"><x-icon name="logout"/>Sign out</button></aside>
<main class="app-main"><header class="topbar"><button class="icon-button mobile-menu" aria-label="Open navigation" [attr.aria-expanded]="drawer()" (click)="drawer.set(true)"><x-icon name="menu"/></button><div><b>Secure file workspace</b><small>Compress, restore and manage your files</small></div><div class="topbar-user"><div class="avatar">{{initials()}}</div><div><b>{{name()||'Your account'}}</b><small>{{email()}}</small></div></div></header><section class="content"><router-outlet/></section></main></div>`})
export class ShellComponent{publicSiteUrl=environment.publicSiteUrl;adminUrl=environment.adminUrl;isAdmin=false;drawer=signal(false);name=signal('');email=signal('');initials=signal('XA');constructor(public auth:AuthService,api:ApiService){api.me().subscribe({next:u=>{this.isAdmin=u.is_admin;this.name.set(u.display_name||'');this.email.set(u.email);const source=u.display_name||u.email;this.initials.set(source.split(/[\s@]+/).filter(Boolean).slice(0,2).map(x=>x[0].toUpperCase()).join(''))},error:()=>this.isAdmin=false})}close(){this.drawer.set(false)}readonly nav=[{path:'/dashboard',label:'Dashboard',icon:'dashboard'},{path:'/compress',label:'Compress',icon:'compress'},{path:'/inbox',label:'Decompress',icon:'decompress'},{path:'/files',label:'History',icon:'history'},{path:'/shares',label:'Secure sharing',icon:'share'},{path:'/security',label:'Security',icon:'security'},{path:'/settings',label:'Account',icon:'user'}]}
