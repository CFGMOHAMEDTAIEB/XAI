import {Component,signal} from '@angular/core';import {DatePipe} from '@angular/common';import {RouterLink} from '@angular/router';import {ApiService} from '../core/api.service';import {FileItem} from '../models/models';import {IconComponent} from '../ui/icon.component';
@Component({standalone:true,imports:[RouterLink,DatePipe,IconComponent],template:`<div class="heading"><div><p class="eyebrow">Your workspace</p><h1>Welcome back{{firstName()? ', '+firstName():''}}</h1><p>Compress, restore and manage your files securely.</p></div><div class="heading-actions"><a class="primary" routerLink="/compress"><x-icon name="compress"/>Compress a file</a><a class="outline" routerLink="/inbox"><x-icon name="decompress"/>Decompress .xaic</a></div></div>
<section class="metrics" aria-label="File overview"><article><div class="metric-head"><span>Files processed</span><x-icon name="file"/></div><strong>{{files().length}}</strong><small>Stored file records</small></article><article><div class="metric-head"><span>Completed</span><x-icon name="check"/></div><strong>{{completed()}}</strong><small>Successful records</small></article><article><div class="metric-head"><span>Failed</span><x-icon name="security"/></div><strong>{{failed()}}</strong><small>Failed records</small></article><article><div class="metric-head"><span>Recent jobs</span><x-icon name="history"/></div><strong>{{recent().length}}</strong><small>Latest activity shown below</small></article></section>
<div class="columns"><article class="panel"><div class="panel-header"><div><h2>Recent activity</h2><p class="muted">Your latest compression records.</p></div><button class="icon-button" aria-label="Refresh activity" [disabled]="loading()" (click)="load()"><x-icon name="refresh"/></button></div>@if(loading()){<div class="skeleton" role="status"><span></span><span></span><span></span></div>}@else if(error()){<div class="error" role="alert">{{error()}}</div>}@else if(!recent().length){<div class="empty-state"><x-icon name="history"/><h3>No activity yet</h3><p>Your compression and restoration activity will appear here.</p><a class="primary" routerLink="/compress">Compress your first file</a></div>}@else{@for(f of recent();track f.id){<div class="activity"><span class="file-icon"><x-icon name="file"/></span><div><b>{{f.name}}</b><small>{{f.codec}}</small></div><span class="badge" [class.success]="f.status==='completed'">{{f.status}}</span><time>{{f.created_at|date:'mediumDate'}}</time></div>}}</article>
<article class="panel"><h2>Quick actions</h2><p class="muted">Choose what you want to do next.</p><div class="quick"><a routerLink="/compress"><x-icon name="compress"/><span><b>Compress a file</b><small>Create a verified XAI artifact</small></span></a><a routerLink="/inbox"><x-icon name="decompress"/><span><b>Restore a file</b><small>Decompress a .xaic artifact</small></span></a><a routerLink="/files"><x-icon name="history"/><span><b>View history</b><small>Download previous artifacts</small></span></a><a routerLink="/security"><x-icon name="security"/><span><b>Account security</b><small>Manage XAI Authenticator</small></span></a></div></article></div>`})
export class DashboardPage{files=signal<FileItem[]>([]);recent=signal<FileItem[]>([]);loading=signal(false);error=signal('');firstName=signal('');completed=()=>this.files().filter(x=>x.status==='completed').length;failed=()=>this.files().filter(x=>x.status==='failed').length;constructor(private api:ApiService){api.me().subscribe({next:u=>this.firstName.set((u.display_name||'').trim().split(/\s+/)[0]||'')});this.load()}load(){if(this.loading())return;this.loading.set(true);this.error.set('');this.api.files().subscribe({next:v=>{this.files.set(v);this.recent.set(v.slice(0,5));this.loading.set(false)},error:()=>{this.loading.set(false);this.error.set('Your activity is unavailable right now. Please try again.')}})}}
