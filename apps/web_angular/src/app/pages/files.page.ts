import {Component,OnInit,signal} from '@angular/core';import {DatePipe,DecimalPipe} from '@angular/common';import {ApiService} from '../core/api.service';import {FileItem} from '../models/models';@Component({standalone:true,imports:[DatePipe,DecimalPipe],template:`<div class="heading"><div><h1>Files</h1><p>Verified local and cloud artifacts.</p></div></div><article class="panel table-wrap"><table><thead><tr><th>Name</th><th>Codec</th><th>Original</th><th>Output</th><th>Saving</th><th>Status</th><th>Date</th></tr></thead><tbody>@for(f of files();track f.id){<tr><td><b>{{f.name}}</b><small class="mono">{{f.sha256}}…</small></td><td><span class="badge">{{f.codec}}</span></td><td>{{f.original_size/1024|number:'1.0-1'}} KiB</td><td>{{f.compressed_size/1024|number:'1.0-1'}} KiB</td><td>{{saving(f)|number:'1.1-1'}}%</td><td><span class="badge success">{{f.status}}</span></td><td>{{f.created_at|date:'medium'}}</td></tr>}</tbody></table></article>`})export class FilesPage implements OnInit{files=signal<FileItem[]>([]);constructor(private api:ApiService){}ngOnInit(){this.api.files().subscribe(v=>this.files.set(v))}saving(f:FileItem){return 100*(1-f.compressed_size/f.original_size)}}
