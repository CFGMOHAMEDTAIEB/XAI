import {Component,OnInit,signal} from '@angular/core';import {DatePipe,DecimalPipe} from '@angular/common';import {RouterLink} from '@angular/router';import {ApiService} from '../core/api.service';import {saveBlob} from '../core/download';import {FileItem} from '../models/models';import {IconComponent} from '../ui/icon.component';
@Component({standalone:true,imports:[DatePipe,DecimalPipe,RouterLink,IconComponent],template:`<div class="heading"><div><p class="eyebrow">Activity</p><h1>History</h1><p>Review and download your compression artifacts.</p></div><button class="outline" [disabled]="loading()" (click)="load()"><x-icon name="refresh"/>Refresh</button></div>@if(loading()){<div class="panel skeleton" role="status"><span></span><span></span><span></span></div>}@else if(message()){<div class="error" role="alert">{{message()}}</div>}@else if(!files().length){<article class="panel empty-state"><x-icon name="history"/><h3>No activity yet</h3><p>Your compression and restoration activity will appear here.</p><a class="primary" routerLink="/compress"><x-icon name="compress"/>Compress your first file</a></article>}@else{<article class="panel table-wrap"><table><thead><tr><th>File</th><th>Operation</th><th>Date</th><th>Status</th><th>Original</th><th>Result</th><th><span class="sr-only">Action</span></th></tr></thead><tbody>@for(f of files();track f.id){<tr><td><b>{{f.name}}</b><small>{{f.codec}}</small></td><td><span class="badge"><x-icon name="compress"/>Compress</span></td><td>{{f.created_at|date:'medium'}}</td><td><span class="badge" [class.success]="f.status==='completed'" [class.danger]="f.status==='failed'">{{f.status}}</span></td><td>{{f.original_size/1024|number:'1.0-1'}} KiB</td><td>{{f.compressed_size/1024|number:'1.0-1'}} KiB</td><td><button class="icon-button" aria-label="Download {{f.name}}" [disabled]="downloading()!==null" (click)="download(f)"><x-icon name="download"/></button></td></tr>}</tbody></table></article>}`})
export class FilesPage implements OnInit{files=signal<FileItem[]>([]);loading=signal(false);message=signal('');downloading=signal<number|null>(null);constructor(private api:ApiService){}ngOnInit(){this.load()}load(){if(this.loading())return;this.loading.set(true);this.message.set('');this.api.files().subscribe({next:v=>{this.files.set(v);this.loading.set(false)},error:()=>{this.loading.set(false);this.message.set('History is unavailable right now. Please try again.')}})}download(f:FileItem){if(this.downloading()!==null)return;this.downloading.set(f.id);this.api.download(f.id).subscribe({next:b=>{saveBlob(b,f.name+'.xaic');this.downloading.set(null)},error:()=>{this.downloading.set(null);this.message.set('The artifact could not be downloaded.')}})}}
