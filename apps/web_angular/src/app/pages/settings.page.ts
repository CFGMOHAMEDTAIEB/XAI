import {Component,signal} from '@angular/core';import {DatePipe} from '@angular/common';import {RouterLink} from '@angular/router';import {ApiService} from '../core/api.service';import {IconComponent} from '../ui/icon.component';
type Profile={email:string;display_name:string;full_name:string;phone_number:string|null;email_verified:boolean;phone_verified:boolean;account_status:string;mfa_enabled:boolean};
@Component({standalone:true,imports:[RouterLink,DatePipe,IconComponent],template:`<div class="heading"><div><p class="eyebrow">Your account</p><h1>Account & settings</h1><p>Manage your profile, verification and account security.</p></div><button class="outline" [disabled]="busy()" (click)="load()"><x-icon name="refresh"/>Refresh account</button></div>@if(busy()&&!profile()){<div class="account-grid"><div class="card skeleton"><span></span><span></span></div><div class="card skeleton"><span></span><span></span></div></div>}@else if(message()){<div class="error" role="alert">{{message()}}</div>}@else if(profile();as p){<div class="account-grid">
<article class="card"><div class="card-title"><x-icon name="user"/><div><h2>Profile</h2><p class="muted">Your personal account information.</p></div></div><div class="detail-list"><div class="detail-row"><span>Full name</span><strong>{{p.full_name||p.display_name||'Not provided'}}</strong></div><div class="detail-row"><span>Email</span><strong>{{p.email}}</strong><span class="badge" [class.success]="p.email_verified">{{p.email_verified?'Verified':'Unverified'}}</span></div><div class="detail-row"><span>Phone</span><strong>{{p.phone_number||'Not provided'}}</strong><span class="badge" [class.success]="p.phone_verified">{{p.phone_verified?'Verified':'Unverified'}}</span></div></div><p class="muted">Profile editing is unavailable because the current API does not expose a safe update endpoint.</p></article>
<article class="card"><div class="card-title"><x-icon name="security"/><div><h2>Security</h2><p class="muted">Credentials and additional protection.</p></div></div><div class="detail-list"><div class="detail-row"><span>Password</span><strong>••••••••••••</strong><a class="outline" routerLink="/forgot-password">Change</a></div><div class="detail-row"><span>XAI Authenticator</span><strong>{{p.mfa_enabled?'Enabled':'Not enabled'}}</strong><a class="secondary" routerLink="/security">{{p.mfa_enabled?'Manage':'Set up'}}</a></div><div class="detail-row"><span>MFA status</span><strong></strong><span class="badge" [class.success]="p.mfa_enabled">{{p.mfa_enabled?'Enabled':'Disabled'}}</span></div></div></article>
<article class="card"><div class="card-title"><x-icon name="check"/><div><h2>Account verification</h2><p class="muted">Verified contact methods.</p></div></div><div class="detail-list"><div class="detail-row"><span>Email verification</span><strong>{{p.email_verified?'Verified':'Not verified'}}</strong><span class="badge" [class.success]="p.email_verified">{{p.email_verified?'Verified':'Unverified'}}</span></div><div class="detail-row"><span>Phone verification</span><strong>{{p.phone_verified?'Verified':'Not verified'}}</strong><span class="badge" [class.success]="p.phone_verified">{{p.phone_verified?'Verified':'Unverified'}}</span></div></div>@if(!p.phone_verified){<div class="notice">Phone verification is currently unavailable because SMS delivery is not configured.</div>}</article>
<article class="card"><div class="card-title"><x-icon name="history"/><div><h2>Session & status</h2><p class="muted">Current account information from the API.</p></div></div><div class="detail-list"><div class="detail-row"><span>Account status</span><strong>{{p.account_status}}</strong><span class="badge success">{{p.account_status}}</span></div><div class="detail-row"><span>Last refreshed</span><strong>{{refreshed()|date:'medium'}}</strong></div></div></article></div>}`})
export class SettingsPage{profile=signal<Profile|null>(null);message=signal('');busy=signal(false);refreshed=signal<Date|null>(null);constructor(private api:ApiService){this.load()}load(){if(this.busy())return;this.busy.set(true);this.message.set('');this.api.me().subscribe({next:u=>{this.profile.set(u);this.refreshed.set(new Date());this.busy.set(false)},error:()=>{this.busy.set(false);this.message.set('Your account information is unavailable. Please try again.')}})}}
