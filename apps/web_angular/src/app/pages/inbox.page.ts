import {Component,signal} from '@angular/core';import {FormsModule} from '@angular/forms';import {ApiService} from '../core/api.service';import {saveBlob} from '../core/download';import {IconComponent} from '../ui/icon.component';
@Component({standalone:true,imports:[FormsModule,IconComponent],template:`<div class="heading"><div><p class="eyebrow">Restore</p><h1>Decompress an XAI artifact</h1><p>Validate and restore a .xaic file to its original content.</p></div></div><article class="panel form-panel"><label class="drop-zone" for="restore-file"><x-icon name="decompress"/>@if(file){<span class="selected-file"><x-icon name="file"/><span><b>{{file.name}}</b><small>{{formatBytes(file.size)}}</small></span></span><span>Choose a different artifact</span>}@else{<b>Drag and drop a .xaic file here</b><span>or choose an artifact from your device</span><span class="secondary">Browse files</span>}<input id="restore-file" type="file" accept=".xaic" [disabled]="busy()" (change)="pick($event)"></label><button class="primary" [disabled]="!file||busy()" (click)="restore()"><x-icon name="decompress"/>{{busy()?'Restoring…':'Restore original file'}}</button>@if(message()){<div [class.success-banner]="success()" [class.error]="!success()" role="status">{{message()}}</div>}</article><article class="panel form-panel"><div class="card-title"><x-icon name="share"/><div><h2>Receive a shared artifact</h2><p class="muted">Enter a recipient-bound share code.</p></div></div><label>Share code<input [(ngModel)]="code" [disabled]="busy()" (ngModelChange)="available.set(false)" placeholder="XC-…"></label><button class="secondary" [disabled]="!code||busy()" (click)="redeem()">Check share code</button>@if(available()){<button class="primary" [disabled]="busy()" (click)="downloadShare()"><x-icon name="download"/>Download shared artifact</button>}</article>`})
export class InboxPage{code='';file:File|null=null;busy=signal(false);message=signal('');success=signal(false);available=signal(false);constructor(private api:ApiService){}formatBytes(v:number){return v>=1048576?(v/1048576).toFixed(1)+' MB':(v/1024).toFixed(1)+' KB'}pick(e:Event){this.file=(e.target as HTMLInputElement).files?.[0]??null;this.message.set('')}restore(){if(!this.file||this.busy())return;const file=this.file;this.busy.set(true);this.api.decompress(file).subscribe({next:b=>{this.busy.set(false);saveBlob(b,file.name.replace(/\.xaic$/i,'')||'restored.bin');this.success.set(true);this.message.set('Integrity verified. Your restored file has been downloaded.')},error:()=>this.fail('This artifact could not be validated or restored.')})}downloadShare(){if(this.busy())return;this.busy.set(true);this.api.downloadShare(this.code).subscribe({next:b=>{this.busy.set(false);saveBlob(b,'shared.xaic');this.available.set(false);this.success.set(true);this.message.set('The shared artifact has been downloaded.')},error:()=>this.fail('The shared artifact is unavailable or its download limit was reached.')})}redeem(){if(this.busy())return;this.busy.set(true);this.available.set(false);this.api.redeem(this.code).subscribe({next:()=>{this.busy.set(false);this.available.set(true);this.success.set(true);this.message.set('Share available. You can download the artifact.')},error:()=>this.fail('The share code is unavailable for this account.')})}private fail(v:string){this.busy.set(false);this.success.set(false);this.message.set(v)}}
