import {Component,signal} from '@angular/core';import {FormsModule} from '@angular/forms';import {JsonPipe} from '@angular/common';import {ApiService} from '../core/api.service';@Component({standalone:true,imports:[FormsModule,JsonPipe],template:`<div class="heading"><div><h1>Inbox</h1><p>Redeem a secure access code.</p></div></div><article class="panel form-panel"><label>Share code<input [(ngModel)]="code" placeholder="XC-ABCD-EFGH-IJKL"></label><button class="primary" [disabled]="!code" (click)="redeem()">Redeem</button>@if(result()){<pre>{{result()|json}}</pre>}</article>`})export class InboxPage{code='';result=signal<unknown>(null);constructor(private api:ApiService){}redeem(){this.api.redeem(this.code).subscribe({next:x=>this.result.set(x),error:e=>this.result.set({error:e instanceof Error?e.message:'Backend unavailable'})})}}
