import {Component,signal} from '@angular/core';
import {FormsModule} from '@angular/forms';import {Router,RouterLink} from '@angular/router';import {AuthService} from '../core/auth.service';
@Component({standalone:true,imports:[FormsModule,RouterLink],template:`<main class="auth-page"><section class="auth-card"><h1>Create account</h1><p>Account and uploaded-file data are processed by the backend. Retention and deletion procedures are not finalized. Review the public privacy draft before registration.</p><form (ngSubmit)="submit()"><label>Email<input type="email" name="email" [(ngModel)]="email" required></label><label>Password<input type="password" name="password" [(ngModel)]="password" minlength="10" required></label><button class="primary" [disabled]="busy()">Create account</button>@if(error()){<p>{{error()}}</p>}</form><a routerLink="/login">Sign in</a></section></main>`})
export class RegisterPage{email='';password='';busy=signal(false);error=signal('');constructor(private auth:AuthService,private router:Router){}async submit(){if(this.busy())return;this.busy.set(true);try{await this.auth.register(this.email,this.password);await this.router.navigateByUrl('/security')}catch{this.error.set('Registration failed. Check your input or sign in if already registered.')}finally{this.busy.set(false)}}}
