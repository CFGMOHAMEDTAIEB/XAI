import Link from 'next/link';
import {SectionHeading} from '@/components/section-heading';
export const metadata={title:'Security'};
export default function Security(){return <section className="page-hero"><div className="container narrow"><SectionHeading eyebrow="Implementation and limitations" title="Security boundaries" body="The API enforces account authorization, administrator roles, upload limits and TOTP authentication when enabled."/><h2>File processing</h2><p>Cloud processing integrates ClamAV and YARA checks. Required scanner failures block processing; architecture does not establish that a scanner is currently operational. <Link href="/status">View current readiness</Link>.</p><p>Compression uses reconstruction and SHA-256 integrity checks. These checks are not encryption and do not guarantee that a file is harmless.</p><h2>Sharing and storage</h2><p>Share codes are restricted to a recipient account, expiration and download count. The backend processes file contents and stores artifacts. End-to-end encryption, zero-knowledge storage and managed envelope encryption are not verified capabilities.</p><h2>Identity and assurance</h2><p>The current API uses password authentication, access and refresh tokens, and TOTP. Enterprise SSO, passkeys and external security certification are not established by this implementation.</p><Link href="/privacy">Read data-processing information and unresolved privacy requirements</Link></div></section>}
