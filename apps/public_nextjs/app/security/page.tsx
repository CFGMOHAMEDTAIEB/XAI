import {ShieldCheck,KeyRound,ScanSearch,FileLock2,ScrollText} from 'lucide-react';import {SectionHeading} from '@/components/section-heading';
export const metadata={title:'Security'};const items=[{i:ScanSearch,t:'Content inspection',d:'MIME validation, ClamAV scanning and YARA policy integration before cloud processing.'},{i:ShieldCheck,t:'Integrity validation',d:'Container checks, payload validation, model fingerprints and original SHA-256 verification.'},{i:FileLock2,t:'Encryption boundary',d:'Compression precedes AES-GCM envelope encryption with keys managed outside application code.'},{i:KeyRound,t:'Strong identity',d:'OIDC, TOTP, passkeys, role-based access and step-up authentication for sensitive operations.'},{i:ScrollText,t:'Auditable actions',d:'Authentication, upload, compression, sharing and administrative actions produce reviewable audit events.'}];export default function Security(){return <section className="page-hero"><div className="container"><SectionHeading eyebrow="Security by design" title="Protect files, identities and processing boundaries" body="The target architecture combines integrity checks, malware scanning, encryption, short-lived authorization and evidence-rich auditing."/><div className="feature-grid">{items.map(({i:I,t,d})=><article key={t}><I/><h3>{t}</h3><p>{d}</p></article>)}</div><div className="notice-box"><b>Prototype scope</b><p>SHA-256 validation, safer container handling, TOTP and temporary share-code controls exist in the MVP. Full Vault-managed encryption, production sandboxing and third-party security assessment remain deployment requirements.</p></div></div></section>}
