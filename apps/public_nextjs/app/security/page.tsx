import Link from 'next/link';import {SectionHeading} from '@/components/section-heading';
export const metadata={title:'Security',description:'Account, file-processing and integrity controls implemented by XAI-Compress.'};
export default function Security(){return <section className="page-hero"><div className="container narrow"><SectionHeading eyebrow="Protection by design" title="Security around the workflow" body="XAI-Compress combines verified accounts, administrator role enforcement, upload boundaries and optional authenticator MFA."/><h2>File processing</h2><p>Cloud processing integrates ClamAV and YARA checks. Required scanner failures block processing rather than releasing unverified output.</p><p>Compression uses reconstruction and SHA-256 integrity checks. Integrity checks are not encryption and do not establish that content is harmless.</p><h2>Sharing and storage</h2><p>Share codes are restricted by recipient account, expiration and download count. The backend processes file contents and stores artifacts; end-to-end encrypted storage is not claimed.</p><h2>Identity and assurance</h2><p>Verified account states, password hashing, access and refresh tokens, and staged TOTP are implemented. Enterprise SSO, passkeys and external certification are not claimed.</p><Link className="text-link" href="/privacy">Read data-processing information →</Link></div></section>}
