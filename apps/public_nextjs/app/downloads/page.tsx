import {Download,Monitor,ShieldCheck,Terminal} from 'lucide-react';import {SectionHeading} from '@/components/section-heading';export const metadata={title:'Downloads'};const downloads=[{i:Monitor,t:'Desktop application',p:'Windows · Linux · macOS',d:'Local static/neural compression, verified decompression, history and secure file receipt.'},{i:ShieldCheck,t:'Mobile Authenticator',p:'Android · iOS',d:'TOTP every 30 seconds, QR enrollment, secure storage and biometric protection.'},{i:Terminal,t:'Command-line engine',p:'Python · Rust extension',d:'Training, compression, decompression, tests, inspection and reproducible benchmarks.'}];export default function Downloads(){return <section className="page-hero"><div className="container"><SectionHeading eyebrow="Applications" title="Choose the interface for each workflow" body="Development builds are distributed from signed CI releases after tests and security scans."/><div className="download-grid">{downloads.map(({i:I,t,p,d})=><article key={t}><I/><h2>{t}</h2><b>{p}</b><p>{d}</p><button className="button" disabled><Download size={17}/>Release pending</button></article>)}</div><div className="notice-box"><b>No public production binary yet</b><p>Build the current source locally. Before public distribution, add code signing, SBOM generation, dependency scanning, release provenance and an update strategy.</p></div></div></section>}
