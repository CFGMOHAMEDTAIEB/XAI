import {SectionHeading}from'@/components/section-heading';export const metadata={title:'Status'};const services=['Public website','FastAPI gateway','Identity provider','Compression workers','Object storage','Security scanner','Notification service'];export default function Status(){return <section className="page-hero"><div className="container narrow"><SectionHeading eyebrow="Development environment" title="Platform service status" body="This public page currently displays declared development status. Connect it to a read-only monitoring endpoint before production."/><div className="status-list">{services.map(x=><div key={x}><b>{x}</b><span className="status-pill">Development</span></div>)}</div><div className="notice-box"><b>Monitoring target</b><p>Grafana and ELK collect operational metrics and logs, while the public endpoint exposes only non-sensitive health information.</p></div></div></section>}
