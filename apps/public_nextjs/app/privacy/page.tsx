import {SectionHeading} from '@/components/section-heading';
export const metadata={title:'Privacy'};
export default function Privacy(){return <section className="page-hero"><div className="container narrow"><SectionHeading eyebrow="Data-processing disclosure — policy draft" title="Privacy and data use" body="This describes the current source implementation. Operator identity, contact details, retention periods and legal terms still require operator review; this is not a finalized privacy policy."/><h2>Accounts and authentication</h2><p>The API stores account email, display name, a password hash, role, MFA state and TOTP enrollment information, including secrets needed for verification. Refresh tokens are stored as hashes. The web portal stores access and refresh tokens in browser local storage. The admin session holds tokens in its server-side circuit.</p><h2>Files and activity</h2><p>Cloud processing sends file contents to the backend for scanning, compression or decompression. Stored records include filenames, sizes, checksums, codec, status, ownership and artifact locations. Sharing stores recipient email, code hashes, expiration and download counters. Audit records contain account identifiers, actions, resources, results and timestamps.</p><h2>Email and devices</h2><p>Configured email providers process delivery addresses and message contents for enrollment or file email. Mobile authenticator accounts and TOTP secrets use device secure storage. Desktop settings and history are stored locally; cloud workflows transmit data to the configured API.</p><h2>Unresolved operator decisions</h2><p>No verified automatic retention schedule or self-service account deletion flow is provided. Share-code expiry does not delete stored files. Hosting access logs, IP metadata, backups, provider retention, data location, deletion requests and an operator contact must be documented before production approval.</p><p>No compliance certification, end-to-end encryption or guaranteed deletion period is asserted here.</p></div></section>}
