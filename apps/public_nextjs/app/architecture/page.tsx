import {SectionHeading} from '@/components/section-heading';import {Upload,ShieldCheck,ScanSearch,BrainCircuit,Archive,Send,FileCheck2} from 'lucide-react';
export const metadata={title:'How XAI-Compress works',description:'The secure product workflow behind XAI-Compress: intake, analysis, lossless compression, protected transfer and verified restoration.'};
const flow=[['Secure intake','A file enters a controlled processing workflow with clear ownership and size boundaries.',Upload],['Safety validation','Required scanners validate input before processing. If validation is unavailable, cloud processing stops safely.',ShieldCheck],['Intelligent analysis','XAI evaluates the file and selects from implemented compression strategies based on its characteristics.',BrainCircuit],['Lossless compression','The selected workflow reduces the representation while retaining the information required for exact restoration.',ScanSearch],['XAI container','The compressed result is packaged as an XAI artifact for storage, download or recipient-bound sharing.',Archive],['Storage or transfer','The artifact can move through the web file workflow while access and audit controls remain in place.',Send],['Verified restoration','Decompression restores the file and integrity checks compare the reconstructed output with its expected hash.',FileCheck2]] as const;
export default function Architecture(){return <><section className="page-hero workflow-hero"><div className="container"><SectionHeading eyebrow="Product workflow" title="From original file to verified restoration" body="XAI-Compress combines controlled intake, adaptive compression and integrity-aware restoration in one understandable path."/><div className="workflow-map"><div className="workflow-file">Original file</div>{flow.map(([title,body,Icon],i)=><article key={String(title)}><span>{i+1}</span><Icon/><div><h2>{String(title)}</h2><p>{String(body)}</p></div></article>)}<div className="workflow-file restored">Original file restored</div></div></div></section><section className="section muted"><div className="container"><SectionHeading eyebrow="Why it matters" title="Less to store and transfer. Nothing intentionally lost." body="Actual compression savings depend on the input, but the product objective stays consistent: efficient movement with exact recovery."/><div className="benefit-grid"><article><h3>Lower footprint</h3><p>Reduce stored and transferred bytes when the selected lossless codec benefits the source.</p></article><article><h3>Exact recovery</h3><p>Lossless workflows preserve the information needed to reconstruct the original file.</p></article><article><h3>Controlled automation</h3><p>Analysis and strategy selection reduce manual codec decisions while retaining explicit failure states.</p></article><article><h3>Security controls</h3><p>Authentication, required safety checks and integrity validation surround the file workflow.</p></article></div></div></section></>}
