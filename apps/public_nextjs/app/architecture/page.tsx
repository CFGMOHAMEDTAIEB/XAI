import {SectionHeading} from '@/components/section-heading';
export const metadata={title:'Architecture'};
const layers=[['Applications','Next.js public website, Angular portal, Flutter desktop and authenticator, server-side Blazor admin.'],['Authentication','FastAPI password authentication, JWT access tokens, refresh tokens and TOTP.'],['File processing','FastAPI invokes the compression engine and ClamAV/YARA checks; cloud processing requires scanner readiness.'],['Data','SQLAlchemy account, file, share and audit records; artifacts on the backend filesystem. Persistence depends on deployment storage.'],['Research','Python compression engine with static/neural workflows and native components. Model availability is a deployment prerequisite.']];
export default function Architecture(){return <section className="page-hero"><div className="container"><SectionHeading eyebrow="Current source architecture" title="Applications and service boundaries" body="This describes the repository implementation, not a guarantee of deployed availability."/><div className="architecture-stack">{layers.map(([a,b],i)=><div key={a}><span>{i+1}</span><h3>{a}</h3><p>{b}</p></div>)}</div><p>Keycloak, a worker queue, MinIO, managed Vault encryption and a monitoring history service are not integrated production dependencies of the current API.</p></div></section>}
