import {portalUrl} from '@/lib/deployment';
import Link from 'next/link';import {ShieldCheck} from 'lucide-react';
const links=[['Features','/#features'],['Security','/security'],['Architecture','/architecture'],['Docs','/docs'],['Pricing','/pricing'],['Downloads','/downloads']];
export function Header(){return <header className="site-header"><div className="container nav"><Link className="brand" href="/"><span className="brand-mark">XC</span><span>XAI-Compress<small>Lossless compression research.</small></span></Link><nav>{links.map(([a,b])=><Link key={b} href={b}>{a}</Link>)}</nav><div className="nav-actions"><Link className="ghost" href="/status"><ShieldCheck size={17}/>Status</Link><a className="ghost" href={`${portalUrl}/login`}>Login</a><a className="ghost" href={`${portalUrl}/register`}>Register</a><a className="button small" href={portalUrl}>Open cloud portal</a></div></div></header>}
