import {portalUrl} from '@/lib/deployment';
import Link from 'next/link';
const links=[['Features','/#features'],['Security','/security'],['How it works','/architecture'],['Docs','/docs'],['Pricing','/pricing'],['Downloads','/downloads']];
export function Header(){return <header className="site-header"><div className="container nav"><Link className="brand" href="/"><span className="brand-mark">XC</span><span>XAI-Compress<small>Lossless compression, clearly delivered.</small></span></Link><nav aria-label="Main navigation">{links.map(([a,b])=><Link key={b} href={b}>{a}</Link>)}</nav><div className="nav-actions"><a className="ghost" href={`${portalUrl}/login`}>Sign in</a><a className="ghost" href={`${portalUrl}/register`}>Create account</a><a className="button small" href={portalUrl}>Open portal</a></div></div></header>}
