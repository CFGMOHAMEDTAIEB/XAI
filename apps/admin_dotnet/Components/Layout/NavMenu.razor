@inject IConfiguration Configuration
<nav class="nav-menu">
    @if (!string.IsNullOrWhiteSpace(Configuration["PortalUrl"])) { <a href="@Configuration["PortalUrl"]">Cloud portal</a> }
    <NavLink href="/" Match="NavLinkMatch.All"><span>▦</span> Dashboard</NavLink>
    <NavLink href="/users"><span>♙</span> Users</NavLink>
    <NavLink href="/jobs"><span>⇅</span> Compression jobs</NavLink>
    <NavLink href="/incidents"><span>⚠</span> Security incidents</NavLink>
    <NavLink href="/quarantine"><span>⌧</span> Quarantine</NavLink>
    <NavLink href="/audit"><span>≡</span> Audit logs</NavLink>
    <NavLink href="/system"><span>⚙</span> System health</NavLink>
</nav>
